- name: test
  hosts: all
  become: true
  tasks:
    - name: Install agent
      include_role:
        name: "{{ lookup('env', 'MOLECULE_PROJECT_DIRECTORY') | basename }}"
      vars:
        version: "2.2.3"
        # TODO: actually deploy an opamp platform
        protocol: opamp
        endpoint: http://localhost:3001
        secret_key:  "{{ lookup('env', 'OIQ_SECRET_KEY') }}"
